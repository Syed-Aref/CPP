(*)string s = "Aref";
(*)Header: #include <cstring>
(*)cout<<s[2]<<endl; // will print the third character( 0 based index )which is 'e'//
(*)s[0] = 'a';
   cout<<s<<endl;// output: aref //
(*)Length: string_var.length();
(*)
  -->
  string str = "Syed Aref Ahmed"; 
  string str1 = "Aref"; 
  int find = str.find(str1);
  cout<<find<<endl;// find = 4//
  -->
  string str = "Syed Aref Ahmed"; 
  int find = str.find('e');// find = 2.'e' appears at index 2,7,13 but will take the first value//
  cout<<find<<endl;
  string str = "Syed Aref Ahmed";  
  int find = str.find(string str,int startPos,int n);
  /*
  startPos = starting position
  n = the number of numbers that will be checked from startPos
  if n = 1, only startPos will be checked
  if n = 2, form startPos to startPos+1 will be checked
  */
  cout<<find<<endl;
  
  
  Note: if the string or character is not found the function will return -1
(*)
   string str="We think in generalities, but we live in details.";
   string str2 = str.substr (3,5);     // "think"
(*)
   Input:
   string s;
   //Only word//
   cin >> s;
   //A line//
   getline(cin,s);
(*)
(*)
(*)
