/**

--> double is more recommended than float
--> #include <cmath>
--> round(num),ceil(num),floor(num) taks double(data type) input and returns integer
--> sqrt(num),cbrt(num) taks double(data type) input and returns double
--> fmax(n1,n2) : n1,n2 can be any data type and retuns the largest
--> fmin(n1,n2) : n1,n2 can be any data type and retuns the smallest

**/
