#include <bits/stdc++.h>
using namespace std;

/***
Typedef is used to give an alternate name to a data type.

Structure: typedef [data type] data_type_alternate name
***/

typedef long long int lli;
typedef pair<int,int> pii;

#define mpr make_pair
#define pbk push_back
#define psh push
#define fir first
#define sec second
#define line "\n"

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    lli a = 3313;
    pii p1 = {23,44};

    cout << a << endl;
    cout << p1.first << " " << p1.second << endl;
}
