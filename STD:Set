#include <iostream>
#include <set> 
#include <iterator> 
using std::cin;
using std::cout;
using std::set;
using std::iterator;
int main()
{
 
    set<int> st;
    //insert//
    st.insert(90);
    st.insert(67);
    //traverse//
    for (auto elem : st) 
    { 
        cout<<elem<<"\n";
    } 
    return 0;
} 
